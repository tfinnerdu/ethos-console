flask==3.1.0
flask-sqlalchemy==3.1.1
sqlalchemy==2.0.36
requests==2.32.3
python-dotenv==1.0.1
gunicorn==23.0.0
jsonschema==4.23.0
psycopg2-binary==2.9.10
uopy==1.4.0

# DOB Repair SQL fetch source (app/dob_sql_source.py) — optional, alternative
# to CSV upload. Also requires the system ODBC driver manager: "ODBC Driver
# 17/18 for SQL Server" + unixODBC on Linux hosts (see Dockerfile). Every
# other DOB Repair path (CSV upload, configured-file reload) works without it.
pyodbc>=5.0.0
